# Moved

The RevenueForge decision log now lives in the planning repo, alongside the rest of the
business's planning documents:

**https://github.com/lloyd-yip/revenueforge** → `docs/planning/DECISIONS.md`
(locally: `~/projects/revenueforge/docs/planning/DECISIONS.md`)

It was moved because only about a third of it documents this model's parameters — the rest is
business planning (pricing architecture, persona, exit strategy, hiring) that used the model as
evidence rather than describing it.

This repo holds the model itself. The reasoning behind its parameters lives with the other
planning documents.
